import 'package:flutter/material.dart';
class NavigationNameRout extends StatelessWidget {
  const NavigationNameRout({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        backgroundColor: Colors.red,
        title: Text("Navigation Route"),
      ),
    );
  }
}
