import 'package:flutter/material.dart';

class StairCasePage extends StatelessWidget {
  const StairCasePage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        backgroundColor: Color(0xd5e50319),
        title: const Text("Civil Quantity Estimator"),
      ),
      body: Center(
        child: Text("18th Page"),
      ),
    );
  }
}