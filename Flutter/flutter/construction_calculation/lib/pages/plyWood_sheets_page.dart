import 'package:flutter/material.dart';

class PlyWoodSheets extends StatelessWidget {
  const PlyWoodSheets({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        backgroundColor: Color(0xd5e50319),
        title: const Text("Civil Quantity Estimator"),
      ),
      body: Center(
        child: Text("15th Page"),
      ),
    );
  }
}